<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recommendedItemCard"
    android:layout_width="162dp"
    android:layout_height="200dp"
    android:layout_margin="16dp"
    app:cardBackgroundColor="@color/light_beige"
    app:cardCornerRadius="24dp"
    app:cardElevation="12dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/recommended_coffee_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:cropToPadding="true"
            android:scaleType="centerCrop"
            tools:src="@drawable/img_recommended" />

        <TextView
            android:id="@+id/coffee_title"
            style="@style/textGeneral"
            android:layout_marginStart="12dp"
            android:layout_marginBottom="46dp"
            android:text="@string/title"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/coffee_price"
            style="@style/textGeneral"
            android:text="@string/rp_20_000"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="@+id/coffee_title"
            app:layout_constraintTop_toBottomOf="@+id/coffee_title" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>